---
const { text, class: className, ...rest } = Astro.props;
---

<p class=`${className} mb-6 subtitle` {...rest}>
  {text}
</p>

<style>
  p {
    font-size: 1.953rem;
    font-weight: 500;
    line-height: 1.15;
    letter-spacing: -0.022em;
  }
</style>
